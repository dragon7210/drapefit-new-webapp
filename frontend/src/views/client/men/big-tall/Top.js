import CTop from '../../components/Top';

const pValue = {
  topClass: 'banner-top-big-tall',
  title: "Men's Big & Tall Fit"
};

const Top = () => {
  return <CTop propsValue={pValue} />;
};

export default Top;
