import CTop from '../../components/Top';

const pValue = {
  topClass: 'banner-top-petite',
  title: 'Personalized Petite Fit'
};

const Top = () => {
  return <CTop propsValue={pValue} />;
};

export default Top;
