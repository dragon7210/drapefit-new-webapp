import CTop from '../../components/Top';

const pValue = {
  topClass: 'banner-top-plus-size',
  title: 'Plus Size Style Fit'
};

const Top = () => {
  return <CTop propsValue={pValue} />;
};

export default Top;
